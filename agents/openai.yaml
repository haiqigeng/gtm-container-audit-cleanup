interface:
  display_name: "GTM Container Audit & Cleanup"
  short_description: "Audit deeply and execute exact GTM cleanup"
  default_prompt: "Use $gtm-container-audit-cleanup to exhaustively audit this GTM container, define its safest target state, and execute and verify only explicitly approved cleanup operations."

policy:
  allow_implicit_invocation: true
